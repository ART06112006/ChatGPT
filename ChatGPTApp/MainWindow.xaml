<Window x:Class="ChatGPTApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatGPTApp"
        mc:Ignorable="d"
        Title="Chat GPT 5.0" Height="700" Width="1250">
    <Grid Background="#28282B">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"/>
            <RowDefinition/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Chats" FontSize="30" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="3" Foreground="White"/>

        <Button x:Name="AddChatButton" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="3" Click="AddChatButton_Click">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border" BorderThickness="3" CornerRadius="10" BorderBrush="Black" Background="#343434" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Label Content="New chat" FontSize="30" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Button.IsMouseOver" Value="True">
                            <Setter Property="Background" TargetName="Border" Value="Gray"/>
                        </Trigger>
                        <Trigger Property="Button.IsPressed" Value="True">
                            <Setter Property="Background" TargetName="Border" Value="LightGray"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <ListBox Grid.Row="1" Grid.Column="0" Grid.RowSpan="2" Background="#28282B" ItemsSource="{Binding Chats, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding CurrentChatTitle, UpdateSourceTrigger=PropertyChanged}">
            <ListBox.ItemTemplate>
                <ItemContainerTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="15*"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Foreground="#818589" Text="{Binding}"/>

                        <Button x:Name="DeleteChatButton" Grid.Column="1" Click="DeleteChatButton_Click" Tag="{Binding}" HorizontalAlignment="Right" VerticalAlignment="Center" Height="20" Width="20">
                            <Button.Background>
                                <ImageBrush ImageSource="../Resources/Delete-Bin-Trash-PNG-Clipart.png"/>
                            </Button.Background>
                        </Button>
                    </Grid>
                </ItemContainerTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button x:Name="ClearButton" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" Margin="5" Click="ClearButton_Click">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border" BorderThickness="3" CornerRadius="10" BorderBrush="Black" Background="#343434" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Label Content="Clear" FontSize="25" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Button.IsMouseOver" Value="True">
                            <Setter Property="Background" TargetName="Border" Value="Gray"/>
                        </Trigger>
                        <Trigger Property="Button.IsPressed" Value="True">
                            <Setter Property="Background" TargetName="Border" Value="LightGray"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <ScrollViewer Grid.Row="1" Grid.Column="1" Margin="5">
            <ItemsControl ItemsSource="{Binding ChatMessages, UpdateSourceTrigger=PropertyChanged}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="10" BorderThickness="0" Margin="5">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="LightGray"/>
                                    <Setter Property="HorizontalAlignment" Value="Left"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Author}" Value="User">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="Background" Value="Gray"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid Margin="3">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="20"/>
                                    <RowDefinition/>
                                    <RowDefinition Height="20"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" FontWeight="Bold">
                                <Run Text="{Binding Author, UpdateSourceTrigger=PropertyChanged}"/>
                                </TextBlock>

                                <TextBlock Grid.Row="1" TextWrapping="Wrap">
                                <Run Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"/>
                                </TextBlock>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Grid Grid.Row="2" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" x:Name="MessageTextBox" Margin="3" Background="#343434" Foreground="White" FontSize="30" HorizontalContentAlignment="Left" VerticalContentAlignment="Center"/>

            <Button Grid.Column="1" x:Name="SendMessageButton" FontSize="20" Margin="3" Click="SendMessageButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" BorderThickness="3" CornerRadius="10" BorderBrush="Black" Background="#343434" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Label Content="Send" FontSize="30" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Button.IsMouseOver" Value="True">
                                <Setter Property="Background" TargetName="Border" Value="Gray"/>
                            </Trigger>
                            <Trigger Property="Button.IsPressed" Value="True">
                                <Setter Property="Background" TargetName="Border" Value="LightGray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Grid>
</Window>
